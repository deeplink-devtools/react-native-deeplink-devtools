/**
 * Input model for the type generator.
 *
 * The generator — emitting a typed route map and a compile-time-checked `buildDeepLink`
 * helper from a {@link RouteTable} — ships in an upcoming release.
 */
export type { Param, Route, RouteTable } from '@deeplink-devtools/core';
