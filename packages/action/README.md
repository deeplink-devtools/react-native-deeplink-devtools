# @deeplink-devtools/action

GitHub Action for `rndl` — planned for the first stable release.

It will run `rndl validate` and `rndl routes --json` in CI, annotate pull requests with
failures inline, and upload SARIF results. Until the CLI commands ship, this package is a
placeholder and is not published.
