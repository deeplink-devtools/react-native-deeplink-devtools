import { Text, View } from 'react-native';

export default function NotificationSettingsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Nested static route at /settings/notifications</Text>
    </View>
  );
}
