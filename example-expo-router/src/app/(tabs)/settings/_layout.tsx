import { Stack } from 'expo-router';

export const unstable_settings = {
  // Legacy spelling, still honored by the router alongside `anchor`.
  initialRouteName: 'index',
};

export default function SettingsLayout() {
  return <Stack />;
}
